# PyTorch CPU-only wheel index.
--extra-index-url https://download.pytorch.org/whl/cpu

# FastAPI web framework for the voice cloning API.
fastapi==0.136.1

# ASGI server for running the FastAPI app.
uvicorn==0.46.0

# Required for FastAPI File/Form uploads.
python-multipart

# Actively maintained Coqui TTS fork with XTTS-v2 support.
coqui-tts==0.25.3

# PyTorch CPU build required by coqui-tts.
torch==2.5.1+cpu

# Audio processing companion to PyTorch, required by coqui-tts.
torchaudio==2.5.1+cpu

# Audio loading, conversion and WAV processing utilities.
pydub==0.25.1

# Async HTTP client for downloading voice notes.
httpx==0.28.1

# Google Text-to-Speech fallback engine.
gTTS==2.5.4

# Environment variable loading from .env files.
python-dotenv==1.2.2

