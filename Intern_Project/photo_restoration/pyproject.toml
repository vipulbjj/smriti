[project]
name = "photo-restoration"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.1",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
]

[tool.vercel]
entrypoint = "vercel_app:app"
